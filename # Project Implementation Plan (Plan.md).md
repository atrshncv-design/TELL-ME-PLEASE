```
# Project Implementation Plan (Plan.md)
**Проект:** Интерактивная платформа «TELL ME PLEASE»
**Статус:** ✅ Платформа полностью готова

## Инструкция для ИИ-агентов
1. Перед началом работы сверьтесь с текущим этапом.
2. Выполняйте задачи строго последовательно.
3. По завершении задачи обновляйте файл `Status.md` (создается отдельно) и ставьте отметку `[x]` в этом файле.
4. После завершения каждого крупного этапа запрашивайте у Functional Validator (пользователя) ручную проверку.

---

## Этап 1: Scaffolding и Базовая Инфраструктура
- [x] 1.1. Инициализация монорепозитория (папки `/frontend` и `/backend`).
- [x] 1.2. Настройка бэкенда: Инициализация Python-окружения, установка FastAPI, Uvicorn, `httpx` (или `aiohttp`).
- [x] 1.3. Настройка фронтенда: Инициализация Next.js (App Router), установка TailwindCSS и Framer Motion.
- [x] 1.4. Настройка линтеров и форматтеров (Prettier, ESLint, Ruff) для поддержания чистоты кода.

## Этап 2: Разработка Backend-ядра (Python + FastAPI)
- [x] 2.1. Создание базового WebSocket-сервера на FastAPI для приема и отправки сообщений.
- [x] 2.2. Реализация **Key Rotation Manager**: скрипт для циклической смены API-ключей OpenCode Zen (считывание из `.env`, перехват ошибок 429/403, retry-логика).
- [x] 2.3. Создание словаря системных промптов (`prompts_config.json`): хардкод-пресеты для 5–9 классов.
- [x] 2.4. Разработка модуля State Machine (Маршрутизатор): скрипт, выбирающий нужный промпт на основе `branch_id`, полученного от клиента.
- [x] 2.5. Реализация "скользящего окна контекста" для обрезки истории диалога (хранение только последних 10 реплик).

## Этап 3: Интеграция LLM и TTS (Streaming Pipeline)
- [x] 3.1. Подключение API OpenCode Zen (`deepseek-v4-flash-free`) в режиме `stream=True`.
- [x] 3.2. Разработка **Sentence Buffer**: скрипт для агрегации потоковых токенов от LLM в полные предложения (по знакам ., !, ?).
- [x] 3.3. Создание `docker-compose.yml` для локального запуска `Kokoro-FastAPI` (TTS).
- [x] 3.4. Настройка клиента для отправки предложений из буфера в Kokoro-82M и получения аудио-чанков.
- [x] 3.5. Интеграция возврата аудио-чанков через WebSocket обратно на клиент.

## Этап 4: Разработка Frontend (UI/UX)
- [x] 4.1. Верстка базового Layout: экраны "Главная (QR)", "Выбор класса", "Выбор раздела".
- [x] 4.2. Верстка экрана "AI Avatar Chat" с геймифицированными анимациями (Framer Motion).
- [x] 4.3. Настройка WebSocket-клиента на фронтенде для связи с FastAPI.

## Этап 5: Голосовой ввод и вывод (Web API)
- [x] 5.1. Интеграция `Web Speech API` для распознавания речи (STT) на клиенте.
- [x] 5.2. Реализация логики `AudioWorklet` для бесшовного потокового воспроизведения аудио-чанков от сервера.
- [x] 5.3. Внедрение защиты от эха: программный mute микрофона во время работы `AudioWorklet`.

## Этап 6: Бизнес-логика сессии и Тестирование
- [x] 6.1. Реализация 3-минутного таймера сессии (синхронизация фронта и бэка).
- [x] 6.2. Настройка триггера "Final Feedback": принудительное завершение диалога сервером с анализом ошибок.
- [x] 6.3. Интеграционное тестирование прерываний (barge-in) и устойчивости менеджера ключей.

```
```
- [x] 6.4. Передача проекта на Human Check (Functional Validator) для итогового тестирования всех классов с 5 по 9.

```
