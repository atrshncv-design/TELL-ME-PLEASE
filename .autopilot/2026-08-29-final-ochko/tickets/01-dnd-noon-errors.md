# 01 — DnD: noon×3, ошибки и задержка экрана

**Требования:** R01, R02, R03, R07
**Blocked by:** —
**Зона:** `content/epochs/future-perfect/`, `frontend/src/components/tasks/DragAndDropTask.tsx`
**Волна:** 1
**Status:** pending

## Что должно заработать

- 3 чипа `noon` с `id noon-1/2/3`, каждый перетаскивается/тапается отдельно (key=`id`).
- `removeEverywhere` и `instantVerdicts` работают по `id`, а не `verb` — второй `noon` не стирает первый.
- После «Проверить» экран не исчезает 1.2с, а ждёт «Далее» (`review:true` для этого задания) — можно рассмотреть ошибки.
- Верная раскладка — все зелёные, неверная — красные по `id` (постоянно красные исправлены).

## Из брифа, дословно

> «В упр. Магия слова целых три слова noon стоит в выборе» / «не так быстро пропадал экран … А то я не успеваю даже рассмотреть свои ошибки» / «И там постоянно горят ошибки, как бы я не делала упражнение»

## Разделы спецификации

Истории 1,2,3.

## Критерии приёмки

- [ ] `station-3.json` 3×`noon` с разными `id`, BY/AT.
- [ ] `DragAndDropTask` использует `id` для key, filter, verdict.
- [ ] `station-3.json` `review:true` — после проверки «Далее».
- [ ] `tsc` 0, `verify` PASS, тапом и drag работает.
