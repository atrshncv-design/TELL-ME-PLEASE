# 01 — Визуально красивое обучение

**Требования:** R01, R02
**Blocked by:** —
**Зона:** `content/epochs/*/index.json`, `frontend/src/components/EpochTheory.tsx`
**Волна:** 1
**Status:** done

## Что должно заработать

- В слайде 5/7 (и аналогичных) заголовки `2.` и `3.` отделены от предыдущего абзаца отступом/разделителем, не слипаются с `…not happy).`.
- Нумерация `1.`–`7.` в Шаге 2/3 — каждый пункт отдельной карточкой/строкой с отступом, склейка `:1.` и `!).2.` устранена.
- Правило TO BE на 5-м слайде уже в рамке — проверить регрессию, не сломать.

## Из брифа, дословно

> «Все таки отмечают плохую разбивку на абзацы (((Как то можно все обучение визуально красиво сделать? … добавить пробелы между разделами или абзацами» + скрины где `клеится not (I am not happy). 2.` и `:1. Действие…!).2.`»

## Разделы спецификации

Истории 1,2; Решения — визуал системно.

## Критерии приёмки

- [x] `EpochTheory` рендерит `\n\n` как `<p>` с `mb-4/6`, `1.` как карточку `bg-amber-50`.
- [x] Заголовки `Шаг 2:`/`Шаг 3:` — `h3` с `mt-6`.
- [x] TO BE рамка на 5-м слайде сохранена.
- [x] `npx tsc` 0, `verify-epoch` PASS, зеркала идентичны.
