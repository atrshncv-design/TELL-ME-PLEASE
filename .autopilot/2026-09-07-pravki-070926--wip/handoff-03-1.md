# Handoff 03-1: ремонт таска 03 (дозапрос 1/2), правок не вносил, дерево зелёное

СДЕЛАНО: расследование всех 3 пунктов ревью (доказательства ниже); код не менял — упёрся в потолок контекста (45 вызовов). tsc+build зелёные на немодифицированном дереве.
ФАЙЛЫ: готовых правок нет; следующий агент правит только frontend/src/components/tasks/*.tsx (QuizTask, VoiceChatTask, OneMinuteTask, SurvivalIslandTask + break-words по списку); тикет .scratch/pravki-070926/issues/03-task-ui.md; интерфейсы .autopilot/2026-09-07-pravki-070926--wip/interfaces.md.
РЕШЕНИЯ: (1) QuizTask:208 чист — в файле нет ни одного input/textarea/placeholder (grep-подтверждено), править нечего; единственное реальное нарушение п.1 — SurvivalIslandTask:348 placeholder="We have a tent..." (текст-образец из шапки в поле ввода) → заменить на нейтральный "Напиши реплику по-английски...". (2) no-mic сообщения уже есть везде: OneMinuteTask:387, SurvivalIslandTask:339, VoiceChatTask:859, EscapeRoomTask:383 — не дублировать; не хватает только строки формулы: в OneMinuteTask (ready-фаза, перед "Таймер 60 секунд" ~строка 247) добавить точную "🎤 Нажми кнопку микрофона и успей рассказать за {duration} секунд, затем нажми «Готово»" (duration в контенте всегда 60, дефолт 60); в VoiceChatTask checklist-карточку (~строка 592, после кнопки "Повторить вопрос") и SurvivalIsland ready-фазу (~строка 500, перед CTA) — честные адаптированные формулы БЕЗ "60 секунд" (там нет таймера; в Survival финал "Готово 🏝️", в checklist — "Следующий вопрос"). (3) break-words уже есть: Quiz:211, FillIn:271, BossBattle:561, EscapeRoom:438, VoiceChat:578/585; добавить на главный текст вопроса: Matching:198, ClickMistake:347, Wheel:272, SentenceBuilder:253, ClozeText:327, GrammarBattle:300, Survival:272, OneMinute:225+242, PromptCard:70 (replaceAll leading-relaxed покроет и 92), EscapeRoom:329 + replaceAll 409/423, TextFix:236, Quiz: replaceAll leading-snug (270+357), VoiceChat: replaceAll "rounded-xl px-3 py-2 text-sm" (673+682+690), BuildChat: replaceAll leading-snug + мультлайн 341-342, Flashcards: обе грани 260/272 + max-h-full overflow-y-auto (фикс h-64, длинный текст режется), Ladder:107, DragAndDrop:221, BuildSentence:276; ChooseStory: replaceAll leading-relaxed (312+380+402) + 370. StickerReaction — pointer-events-none, 1.3с, по дизайну (тикет 05): не трогать. Sticky-бары DragAndDrop:277/TextFix:332 — G3-дизайн, не трогать.
ТУПИКИ: rg отсутствует в shell (использовать Grep-тул); "60 секунд" нельзя писать в Survival/VoiceChecklist/EscapeRoom-code-phrase — там нет таймера, это ложь ученику; TaskHeader:22 truncate — шапка, не вопрос, не трогать; контентные JSON и зеркала — запрет, durations в контенте везде 60.
ДАЛЬШЕ: внести правки выше (~28 точечных edit), затем cd frontend && npx tsc --noEmit && npm run build; отчёт РОВНО блоком STATUS/FILES/TESTS/INTERFACES/REQUIREMENTS/CONCERNS/BLOCKERS ≤25 строк, REQUIREMENTS: R13/R15/R17.
