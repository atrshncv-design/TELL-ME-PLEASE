# Границы и швы: Правки 070926

Скопировано из spec.md §«Границы и швы» — Phase 4 копирует отсюда дословно.

| Модуль | Владеет | Выставляет | Прячет |
|---|---|---|---|
| Страница эпохи | портал-%, достижения, секторы, кнопка печати, «Продолжить» | прогресс из ключей классов, якоря секторов | чтение localStorage, проверку PDF |
| Сектор/станция | список станций, шаг «Дальше», возврат | вычисление следующего stationId из индекса | роутинг деталей |
| Задачи (типы) | задание сверху, поле только для ответа, листалка N/M, голосовые поля | единый контракт «задание/ответ/завершение» | внутреннюю механику типов |
| Контент эпох | тексты станций в двух зеркалах | JSON-индексы и файлы станций | ничего — данные |
| Экзамен | список, позиция, метки сессии | те же швы навигации, что у эпох | хранилище меток сессии |

Швы для тестов: валидаторы контента, проверка типов, прод-сборка,
побайтовое сравнение зеркал. Новых швов не вводим.

НЕ ТРОГАТЬ (из памяти проекта): серверный чат и API, zscripts, контент без
задачи; новая зависимость = BLOCKED.

## Из таска 01 — страница эпохи

- `nextHref` кросс-секторный `/epoch/<slug>/<sector>/<stationId>` (null на
  последней станции эпохи); якоря `#sector-<id>` на карточках секторов
- Печать: `/pdfs/<slug>.pdf` + `-bw-a4/-a5/-bw-a5`, Range-проверка, нет файла → disabled «Скоро»
- Владелец страницы эпохи и сектора: таск 01 (блок достижений — таск 02)

## Из таска 03 — экран задания

- Контракт задание/ответ/завершение: `onComplete(score,total)` один раз на финише; листалка `goTo(i)` не пишет score
- Игровые типы (EscapeRoom/BossBattle/GrammarBattle/Ladder/ClickMistake) — осознанно последовательные, preview нет
- Владелец компонентов заданий и страницы станции: таск 03

## Из таска 02 — достижения (ревью чисто)

- `checkAchievements(progress, typeById, alreadyUnlocked) -> UnlockedAchievement[]`; `typeById` теперь task.id→type
- Владелец библиотеки достижений и блока достижений: таск 02
