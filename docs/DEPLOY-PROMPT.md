# Промпт для агента space.z-ai — деплой time travel mission (v4, билд 60aa2e2)

_Уроки прошлых попыток учтены: см. «КРИТИЧЕСКИЕ ПРАВИЛА». Не отклоняйся от них._

---

Ты — агент платформы space.z-ai. Задача: задеплоить свежую версию проекта time travel mission
и проверить, что она работает. Прошлые попытки проваливались, когда ты копировал файлы
вручную и запускал сервер руками вместо штатного механизма — в этот раз делай всё по правилам.

## КОНТЕКСТ

- Репозиторий: `https://github.com/atrshncv-design/TELL-ME-PLEASE` (ветка `main`).
  **Последний коммит — `60aa2e2`** (`feat(gamification): wave 3 — grammar minecraft, battle,
  boss battle, achievements, memes`). Проверь перед деплоем: `git log --oneline origin/main -1`
  должен показать именно его.
- Проект: Next.js 16 (App Router) — фронт в папке `frontend/` (там package.json).
  Бэкенда нет (FastAPI удалён) — всё в API Routes внутри frontend. Контент в `content/`
  и зеркале `frontend/public/content/tasks/` (обязаны быть синхронны).
- Приложение: платформа английского для детей 5–9 классов. **Это НЕ старая версия — теперь
  это игровая система «Verb Bot Mission»** (3 волны геймификации заказчицы):
  1. Лендинг: «time travel mission», слоган «Учи английский — играя!», кнопка «Начать играть →»
     → `/mission`; есть кнопки миров (Грамматика, Глагол to be, Словарный запас, Аудирование,
     Общение) и кнопка «Мемы» → `/memes`.
  2. `/mission` — брифинг: «Начнём миссию!», заглушка «Таблица времён — скоро!» (12 времён
     в сетке), «Как играть» (4 пункта), «Продолжить →» → `/class`.
  3. Карта миров (выбор класса → секции): счётчики **⚡ Energy / 🗣 Communication / 💎**,
     индикатор **«Портал восстановлен на X%»**, ряд **достижений** (Question Master,
     Past Tense Hero, Speaking Streak). Задания: quiz, fill-in, click-mistake (детектив),
     choose-story, one-minute, survival-island, build-chat, escape-room, build-sentence
     (майнкрафт-блоки), grammar-battle, boss-battle, voice-chat и др.
  Старая версия — «просто заголовок и кнопка „Начать →"» и без валют/портала: если видишь
  её — билд устарел.

## КРИТИЧЕСКИЕ ПРАВИЛА (из ошибок прошлых разов — НЕ НАРУШАЙ)

1. **Используй ТОЛЬКО штатный инструмент деплоя платформы.** НЕ копируй файлы вручную,
   НЕ правь файлы на сервере напрямую — платформа разворачивает проект из `repo.tar` при
   старте контейнера, ручные правки наружу не пробрасываются.
2. **НЕ запускай сервер вручную** (`bun run dev`, `next dev`, `next start` и т.п.) — штатный
   процесс запускается при старте контейнера. Ручной сервер конфликтует и не обслуживает
   внешний трафик.
3. **НЕ создавай временных эндпоинтов** (типа `/api/debug-env`) и НЕ добавляй ничего в
   репозиторий сверх того, что в нём есть. Env проверяй косвенно через `/api/chat/stream`.
4. **Платформа кэширует старый билд на уровне ингресса.** Если после деплоя внешний URL
   отдаёт старую версию — это НЕ чинится ручными правками. Остановись и сообщи пользователю:
   нужно нажать «Restart/Redeploy» в дашборде.
5. **`next dev` (Turbopack) нестабилен на этой платформе** (падает после 1–3 запросов) —
   это известная проблема, не чини её. В репозитории уже настроен production-режим:
   `"dev": "next start -p 3000"` + `"postinstall": "next build"` — этого достаточно, ничего
   не переписывай.
6. **Ключи/пароли — НИКОГДА не выводи в отчёт и не проси у пользователя в чате.** В дашборде
   уже должны быть залиты переменные из `frontend/.env.example`. Если какой-то переменной
   нет — просто скажи пользователю, КАКАЯ отсутствует, а не проси прислать значение.
7. **Если install шёл через bun** — проверь, что `.next/` появился (bun по умолчанию БЛОКИРУЕТ
   postinstall: «Blocked 1 postinstall»). Если `.next/` нет — значит `next build` не выполнился:
   выполни `npm run build` явно (или `bun pm untrusted` и переустанови), затем передеплой.
   Без `.next/` сервер падает — это главный кандидат на «тихий» провал деплоя.

## ШАГИ

1. Проверь свежий `main`: последний коммит `60aa2e2` (геймификация волна 3).
2. Убедись, что проект собирается (локально, ДО деплоя):
   `cd frontend && npx tsc --noEmit` → 0 ошибок; `npm run build` → зелёный.
   Также прогони цикл как на платформе: `rm -rf node_modules package-lock.json && bun install`
   (если bun доступен) — при «Blocked 1 postinstall» выполни `bun pm untrusted` или
   `npm run build` вручную и убедись, что `.next/` существует.
3. Задеплой штатным инструментом платформы.
4. Проверь внешний URL (актуальный — уточни в дашборде; ранее был
   `https://m1ed663b3d70-d.space-z.ai/`):
   - `/` → лендинг «time travel mission» + «Начать играть →» + чипы миров + «Мемы»
     (это НОВАЯ версия; если видишь карту миров сразу на главной или нет «Мемы» — билд устарел);
   - `/mission` → 200, «Начнём миссию!», таблица времён, «Продолжить →»;
   - `/memes` → 200, 5 карточек-шуток;
   - `POST /api/event` с телом `{"device_session_id":"probe12345678","event_type":
     "grade_selected","grade":5}` → `{"status":"ok"}`. Если 404 — старый билд (правило 4);
   - build ID в `.next/BUILD_ID` на новом билде отличается от старого (был `fbCNoYyYfmO80Vl1`,
     затем `WwUNg9FPyNesFZVK1YfbC`, затем третий — на этом должен быть четвёртый).
5. Если URL обновился — протестируй:
   a. `POST /api/chat/stream` с телом `{"branch_id":"5","task_id":"voice_chat_greetings",
      "messages":[{"role":"user","content":"Hello! My name is Tom."}]}` → SSE-поток с
      событиями `token` и `done`. «Сервис не настроен» → не хватает env (скажи пользователю,
      каких); «Сервис перегружен» → очередь работает, это ок.
   b. Пройди путь в браузере: `/` → «Начать играть» → `/mission` → «Продолжить» → выбор
      класса → 5 класс → карта миров (видны ⚡/🗣/💎, «Портал восстановлен на X%»,
      достижения) → открой quiz → выбери ответ → «Проверить» → вердикт с подсказкой → «Далее».
      Новые типы (проверь хотя бы 2): Choose Your Story (выбор героя/места/проблемы →
      рассказ → самооценка), Escape Room (5 станций, двери открываются, «+1 ⚡»),
      Boss Battle (HP-босс, 6 вызовов + план поездки), Grammar Minecraft (блоки +
      «Сменить время»). Кнопка звука 🔊/🔇 в правом верхнем углу на всех страницах.
   c. Админка: `/admin` — страница статистики (или требует логин — приемлемо для MVP,
      отметь что увидел).
6. Собери отчёт: задеплоенный коммит (build ID), результаты каждого теста, что не удалось и
   почему, что требуется от пользователя (какие env отсутствуют / нужен ли Restart/Redeploy).

## ЧТО ПОЛЬЗОВАТЕЛЬ УЖЕ ДОЛЖЕН СДЕЛАТЬ (проверь, напомни)

- В настройках проекта залиты 8 переменных (значения — только у пользователя, не проси их):
  `LLM_API_KEYS`, `LLM_API_BASE`, `LLM_MODELS`, `LLM_QUEUE_MAX`, `LLM_QUEUE_TIMEOUT`,
  `ADMIN_LOGIN`, `ADMIN_PASSWORD`, `EVENTS_FILE`. Шаблон: `frontend/.env.example`.
- После заливки env — нажать «Restart/Redeploy».

Отчитайся по каждому пункту — коротко, фактами. Ключи и пароли в отчёт не включать.

---

_Изменения против v3 (f7e94fb): +3 волны геймификации (коммиты 5f30aa2, f3d7023, 60aa2e2) —
2 валюты ⚡/🗣, портал, умные подсказки, 8 новых типов заданий, достижения, раздел «Мемы»;
контент 5 класса 62 → 67 заданий. Старые маркеры билда заменены._
